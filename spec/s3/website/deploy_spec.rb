RSpec.describe S3::Website::Deploy do
  it "has a version number" do
    expect(S3::Website::Deploy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
